#include<stdio.h>
int main () {
    int x,y,n;
    // code multiplation
    printf("donner deux nombre pour les multiplier\t");
    scanf("%d",x);
    scanf("%d",y);
    n=x*y;
    printf(" la multiplication  est: %d",n);
    return 0;
}